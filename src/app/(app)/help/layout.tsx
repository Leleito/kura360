export const metadata = {
  title: "Help Center",
  description: "Election seat guides, ECFA compliance rules, and platform documentation for KURA360",
};

export default function HelpLayout({ children }: { children: React.ReactNode }) {
  return children;
}
